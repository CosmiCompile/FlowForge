services:
  server:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"

  # Postgres optional (later)
  # db:
  #   image: postgres:16
  #   environment:
  #     POSTGRES_PASSWORD: flowforge
  #   ports:
  #     - "5432:5432"
